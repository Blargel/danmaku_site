%section#project-search
  .page-header
    %h1
      Projects
      - if @user
        %small
          by
          = @user.login

  = will_paginate @projects, :inner_window => 2

  Order: &nbsp
  = order @projects, :by => :created_at, :as => "Created Date"
  &nbsp
  = order @projects, :by => :updated_at, :as => "Updated Date"
  &nbsp
  = order @projects, :by => :title, :as => "Title"
  &nbsp
  = order @projects, :by => :win_votes, :as => "Rating"
  &nbsp
  = order @projects, :by => :downloads, :as => "Downloads"

  - @projects.each_with_index do |p, i|
    = render :partial => 'shared/project', :locals => {:p => p}
    = "<hr />".html_safe unless i == 9

  = will_paginate @projects, :inner_window => 2
